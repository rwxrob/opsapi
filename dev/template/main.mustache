{{>partial_header}}
package main

import (
	"log"

	sw "github.com/rwxrob/opsapi/go"
)

func main() {
	log.Printf("Server started")

	router := sw.NewRouter()

	log.Fatal(router.Run(":{{serverPort}}"))
}
